(()=>{const canvas=document.getElementById('canvas'),ctx=canvas.getContext('2d');let W,H;const resize=()=>{W=canvas.width=innerWidth;H=canvas.height=innerHeight-50};resize();addEventListener('resize',resize);const colorEl=document.getElementById('color');let params;function randomize(){params={f1:1+Math.random()*4|0,f2:1+Math.random()*4|0,f3:1+Math.random()*4|0,f4:1+Math.random()*4|0,p1:Math.random()*Math.PI*2,p2:Math.random()*Math.PI*2,p3:Math.random()*Math.PI*2,p4:Math.random()*Math.PI*2,d1:0.001+Math.random()*0.003,d2:0.001+Math.random()*0.003,a1:Math.min(W,H)*0.35,a2:Math.min(W,H)*0.35};ctx.fillStyle='#0a0a1a';ctx.fillRect(0,0,W,H);t=0;}let t=0;randomize();document.getElementById('randBtn').onclick=randomize;document.getElementById('saveBtn').onclick=()=>{const a=document.createElement('a');a.download='harmonograph.png';a.href=canvas.toDataURL();a.click();};function draw(){const{f1,f2,f3,f4,p1,p2,p3,p4,d1,d2,a1,a2}=params;const steps=50;for(let i=0;i<steps;i++){const x=W/2+a1*Math.sin(t*f1+p1)*Math.exp(-d1*t)+a2*0.5*Math.sin(t*f3+p3)*Math.exp(-d2*t);const y=H/2+a1*Math.sin(t*f2+p2)*Math.exp(-d1*t)+a2*0.5*Math.sin(t*f4+p4)*Math.exp(-d2*t);const mode=colorEl.value;let color;if(mode==='gold')color=`rgba(255,${180+Math.sin(t*0.01)*40|0},50,0.3)`;else if(mode==='rainbow')color=`hsla(${t%360},70%,60%,0.3)`;else if(mode==='blue')color=`rgba(100,${150+Math.sin(t*0.01)*50|0},255,0.3)`;else color='rgba(255,255,255,0.3)';ctx.fillStyle=color;ctx.fillRect(x,y,1.5,1.5);t+=0.02;}if(t<3000)requestAnimationFrame(draw);}draw();})();